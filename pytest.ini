[pytest]
testpaths = tests
addopts = --ignore=tests/smoke_test.py --ignore=tests/test_signal_selection.py
markers =
    network: requires internet access to PhysioNet
    slow: takes more than a few seconds
